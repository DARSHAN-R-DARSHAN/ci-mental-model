{
  "name": "ci-mental-model",
  "version": "1.0.0",
  "scripts": {
    "test": "node node_modules/jest/bin/jest.js --ci --reporters=default --reporters=jest-junit"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  }
}
