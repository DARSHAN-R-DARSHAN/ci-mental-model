name: Learning CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: npm test

      - name: Upload test result
        uses: actions/upload-artifact@v4
        with:
          name: result
          path: result.txt
